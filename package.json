{
  "name": "spy-telegram-web-app",
  "version": "0.0.1-alpha",
  "author": "dev.n@bk.ru Valentin Kruglikov",
  "license": "MIT",
  "source": "./src/index.ts",
  "type": "module",
  "scripts": {
    "build": "rm -rf ./dist/*; microbundle build --sourcemap=false",
    "dev": "npm run build && microbundle watch --compress=false"
  },
  "devDependencies": {
    "microbundle": "^0.15.1"
  }
}
